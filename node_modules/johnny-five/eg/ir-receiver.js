var five = require("../lib/johnny-five.js"),
    board, ir;

board = new five.Board();

board.on("ready", function() {

  // Create a new `ir` hardware instance.
  ir = new five.IR.Receiver(2);

  // IR.Receiver Event API

  // // "read" get the current reading from the ir
  // ir.on("read", function( err, value ) {
  //   console.log( "read", value );
  // });

  // ir.on("change", function( err, value ) {

  //   console.log( typeof this.inches );
  //   console.log( "Object is " + this.inches + "inches away" );
  // });
});
