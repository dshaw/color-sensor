var five = require("../lib/johnny-five.js");

(new five.Board()).on("ready", function() {
  var buttons;

  [ 4 ].forEach(function( pin ) {
    var button = new five.Button({
      pin: pin,
      isPullup: true
    });

    button.on("down", function() {
      console.log( "down" );
    });
  });
});
